    private static object GetPropertyFor{{type}}({{type}} obj, string path)
    {
        switch (path) {
{{get_switch}}
        }
    }
    private static void SetPropertyFor{{type}}({{type}} obj, string path, object value)
    {
        switch (path) {
{{set_switch}}
        }
    }
